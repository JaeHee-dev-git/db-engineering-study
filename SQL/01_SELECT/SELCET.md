# SELECT

## SELECT란?

SELECT는 데이터베이스에서 원하는 데이터를 조회할 때 사용하는 SQL 명령어이다.

---

## 기본 문법

```sql
SELECT 컬럼명
FROM 테이블명;
```

### 예시

```sql
SELECT name
FROM employee;
```

의미

employee 테이블에서 name 컬럼만 조회한다.

---

## 오늘 배운 내용

- SELECT는 데이터를 조회하는 명령어이다.
- FROM 뒤에는 데이터를 가져올 테이블 이름이 온다.